services:
  channel-sentinel:
    image: cgg888/channel-sentinel:latest
    container_name: channel-sentinel
    network_mode: "host"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
      - PORT=${IPTV_PORT:-3000}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
